# Handoff — Sesion 2026-06-25 (segunda parte)

> Ultima actualizacion: 2026-06-25
> Estado: v2.0 completo. 29/30 tickets resueltos. 1 abierto (T-029).

---

## Contexto

Migracion v2.0 completada y corriendo en produccion con `llama-3.3-70b-versatile`
via Groq directo. T-027 (anti-alucinacion listas) implementado: Regla 9 en system
prompt, `_extract_named_items()` + `_check_confidence()` mejorado, modelo 70B,
retriever k=8.

Las respuestas del chatbot son "regulares" — no alucina, pero el tono y la
consistencia necesitan mejora. El system prompt actual no define audiencia ni
profundidad tecnica esperada.

**Documentos rectores:**
- `CONTRATO.md` — Decisiones inamovibles (actualizado 25 junio)
- `AGENTS.md` — Arquitectura v2.0 (actualizado con Groq y 70B)
- `TICKETS.md` — 30 tickets (29 resueltos, 1 abierto)

---

## Progreso

### Completado

- [x] Migracion v2.0 completa (27/27 tickets originales)
- [x] T-027 anti-alucinacion listas implementado (modelo 70B, Regla 9, verificacion)
- [x] CONTRATO.md corregido a estado real (LCEL, k=8, Groq directo, criterios marcados)
- [x] AGENTS.md actualizado (LiteLLM → Groq, llama-8b → 70B)
- [x] `requirements.txt` eliminado
- [x] `perfil_personal.md` entity_name corregido
- [x] `.opencode/` en .gitignore
- [x] T-028 limpieza `Pruebas hechas/` (directorio ya no existia)

### Pendiente

#### T-029 [ ] Sistema de registro de interacciones + tono de audiencia + memoria contextual

3 cambios:
1. `src/interaction_logger.py` (nuevo) — SQLite con escritura asincrona
2. `config/system_role.md` — reglas de audiencia, tono, memoria activa
3. `src/chat.py` — disparar log asincrono tras cada respuesta

Endpoint: `GET /admin/interactions?limit=50` (protegido X-API-KEY)

---

## Como ejecutar y testear

```bash
docker compose up -d
bash update_knowledge.sh
curl -X POST http://localhost:8000/chat -H "Content-Type: application/json" \
     -d '{"question": "Hablame sobre Tradehub", "session_id": "abc123"}'
pytest tests/unit/ -v
python tests/evaluator.py
```

---

## Trampas conocidas

1. Workers=1 obligatorio (ChromaDB SQLite)
2. `data/**/*.md` en .gitignore
3. `entity_name` sin `.md` en frontmatter
4. `_check_confidence()` mejorado verifica items contra contexto
5. Modelo: `llama-3.3-70b-versatile` via `api.groq.com/openai/v1`
6. Retriever k=8, sin deteccion de keywords
7. El tono inconsistente NO es culpa del modelo 70B — es que el system prompt
   no define audiencia ni escalera de profundidad
